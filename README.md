# Test Spec Check Filter Release

E2E test repo for `spec-check-filter-release` workflow in buildon-github-actions.

Tests JSON schema validation against meta-schema, OCI scratch packaging, and t4 token style.
